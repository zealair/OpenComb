module.exports = function()
{
	var view = this ;
	$(this).find('#btnSignOut').click(function(){
		var username = $(view).find("#selUsername").val() ;
		console.log(username)
		$oc.director.requestAndSwitch({
			url: "/ocPlatform/lib/usr/SignOut:do"
			, data:{username:username}
		},view) ;
	}) ;
}
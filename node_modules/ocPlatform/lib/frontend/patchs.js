
if(typeof process=='undefined')
{
	process = {} ;
}

if(!process.nextTick)
{
	process.nextTick = function(callback)
	{
		setTimeout(callback,0) ;
	}
}




var View = require("ocPlatform/lib/mvc/view/View.js") ;
var FrontendView = require("ocPlatform/lib/frontend/mvc/ViewTemplate.js") ;
View.Caches.singleton = FrontendView.Caches.singleton ;